// config/environments/production.js
var ProductionEnvironment = require('ember-cli/environment/production');

module.exports = ProductionEnvironment.extend({

  serverConfig: {
    minify: true
  },

  server: function(config) {
    this._super.apply(this, arguments);

    config.set('sass.outpuPath', 'dashboard.css');
    config.set('minify', true);
  },

  runtime: function(config) {
    config.set('torii.providers', ['google', 'facebook']);
  },

  broccoli: function(app, serverConfig) {
    return app.toTree();
  },
}


// config/environments/staging.js

var Prod = require('./production');

// if same exact setup
module.exports = Prod;

// if customized
module.exports = Prod.extend({

});
